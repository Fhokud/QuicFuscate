/**
 * Runtime feature flags for Web Admin UI.
 *
 * Experimental runtime switches are intentionally disabled in production
 * code paths. Keep this module for future runtime toggles.
 */
export const RUNTIME_FLAGS = Object.freeze({});
